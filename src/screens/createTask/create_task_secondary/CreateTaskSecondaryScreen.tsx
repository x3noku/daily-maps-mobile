import React from 'react';
import { SafeAreaView } from 'react-native-safe-area-context';

const CreateTaskSecondaryScreen = () => {
    return <SafeAreaView style={{ width: '100%', height: '100%' }} />;
};

export default CreateTaskSecondaryScreen;
